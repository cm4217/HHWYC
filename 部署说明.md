# 化学性质预测平台 · 部署与安装说明

**GitHub：** https://github.com/cm4217/HHWYC

化合物理化性质预测平台（HHWYC）是一个纯前端单页应用，所有计算在浏览器内完成（RDKit WASM 引擎离线可用），无需后端数据库。

---

## 一、目录结构

```
HHWYC/
├── index.html                 # 主页面（入口）
├── serve.py                   # 静态服务器（用于 WorkBuddy / 任意支持单端口 HTTP 的云环境）
├── sw.js                     # Service Worker（已停用，保留仅作兼容）
├── manifest.webmanifest       # PWA 清单
├── local_ocr_server.py        # 可选：本地结构式图片 OCR 服务
├── setup_ocr_server.bat       # 本地 OCR 服务一键安装脚本（Windows）
├── setup_ocr_server.ps1       # 本地 OCR 服务一键安装脚本（PowerShell）
├── README.md                  # 功能概述
├── 部署说明.md                # 本文件
├── 调研报告_CAS转SMILES增强.md
├── OVERVIEW_*.md              # 各版本迭代记录
├── .gitignore
└── assets/
    ├── styles.css             # 全站样式
    ├── app.js                 # 主逻辑
    ├── advance.js             # 高级预测
    ├── predict.js             # 预测核心
    ├── pubchem.js             # PubChem 查询
    ├── cas-kb.js              # 化学知识库
    ├── seed-db.js             # 种子数据
    ├── compound-dict.js       # 化合物词典
    ├── examples.js            # 示例
    ├── research-tools.js      # 工艺研发工具箱
    ├── rdkit-engine.js        # RDKit WASM 封装
    ├── icon-192.png / icon-512.png
    ├── rdkit/                 # RDKit WASM 引擎（离线核心）
    │   ├── RDKit_minimal.js
    │   ├── RDKit_minimal.wasm
    │   └── RDKit_minimal.wasm.b64.js
    └── vendor/
        ├── xlsx.full.min.js   # Excel 导出
        └── jszip.min.js       # ZIP 打包依赖
```

> 注意：`node_modules/`（约 28 MB）为 E2E 自动化测试依赖，**非网站运行所需**，已被 `.gitignore` 排除，上传 GitHub 时无需提交。

---

## 二、本地直接打开

由于所有资源均为相对路径，且 RDKit WASM 已内嵌 `RDKit_minimal.wasm.b64.js`，**直接双击 `index.html` 即可在浏览器离线运行**（少数浏览器对 `file://` 下的 WASM 有限制，如遇加载失败请用下方服务器方式）。

---

## 三、本地静态服务器运行

任选其一：

```bash
# 方式 A：Python（推荐，无需安装依赖）
cd HHWYC
python -m http.server 8080 --bind 127.0.0.1
# 浏览器访问 http://127.0.0.1:8080

# 方式 B：本仓库附带的 serve.py（自动读取 PORT 环境变量，适配云部署）
cd HHWYC
python serve.py
# 默认监听 0.0.0.0:3000，可用环境变量 PORT 覆盖
```

---

## 四、部署到 GitHub Pages

1. 在 GitHub 新建仓库（如 `HHWYC`）。
2. 将压缩包解压后的全部文件提交：
   ```bash
   git add .
   git commit -m "feat: import chem property predictor site and docs"
   git push origin main
   ```
   `.gitignore` 已自动排除依赖与缓存。
3. 仓库 **Settings → Pages → Build and deployment → Source** 选择 `Deploy from a branch`，分支选 `main`，目录选 `/ (root)`。
4. 等待约 1 分钟，访问 `https://<用户名>.github.io/<仓库名>/`。

> GitHub Pages 默认站点根目录，资源相对路径 `assets/...` 可正常加载，无需修改 `index.html`。

---

## 五、部署到 WorkBuddy 云端（沙箱）

1. 在 WorkBuddy 对话中要求「发布 / 部署」本目录。
2. 部署工具会自动识别为静态站点并启动 `serve.py`（监听 `0.0.0.0:$PORT`）。
3. 获得分享链接后，浏览器打开即可使用。

---

## 六、可选：启用本地 OCR 结构式识别

默认在线 AI 识别（MolScribe 在线 API）不稳定时，可启用本机 OCR 服务（数据不出内网）：

```bash
# Windows 双击 setup_ocr_server.bat 即可自动安装并启动
# 或手动：
python -m venv venv_ocr
venv_ocr\Scripts\activate
pip install molscribe decimer molnextr pillow rdkit
python local_ocr_server.py
```

启动后在站点「图片识别」页填写 `http://127.0.0.1:5000` 并点击「检测」。

---

## 七、常见问题

| 现象 | 原因与解决 |
|------|-----------|
| 页面无样式 / 只有纯文本 | 检查 `assets/styles.css` 是否被服务器正确返回；若部署平台对带 `?v=` 查询参数的静态资源返回失效，需移除 `index.html` 中的版本戳（本版已移除）。 |
| RDKit 引擎一直「未加载」 | 使用本地服务器（方式三）访问，避免 `file://` 的 WASM 限制；或检查 `assets/rdkit/` 三文件是否完整。 |
| 图片识别失败 | 改用本地 OCR（第六部分）或手动输入 SMILES。 |

---

*本平台预测结果为计算 / 规则近似，仅供研发参考，非监管申报或临床依据。*
